# راهنمای نصب پلاگین قرارداد آنلاین هنر مغز

## پیش‌نیازها

- WordPress 5.0 یا بالاتر
- PHP 7.4 یا بالاتر
- Composer (برای نصب کتابخانه‌های PHP)

## مراحل نصب

### 1. آپلود پلاگین

فایل‌های پلاگین را در مسیر زیر قرار دهید:
```
wp-content/plugins/honar-contract/
```

### 2. نصب وابستگی‌های PHP با Composer

در ترمینال، به مسیر پلاگین بروید و دستور زیر را اجرا کنید:

```bash
cd wp-content/plugins/honar-contract/
composer install
```

این دستور کتابخانه‌های زیر را نصب می‌کند:
- `dompdf/dompdf` - برای تبدیل HTML به PDF
- `phpoffice/phpword` - برای کار با فایل‌های Word
- `sallaert/jalali-moment` - برای تبدیل تاریخ میلادی به شمسی

### 3. فعال‌سازی پلاگین

1. وارد پنل مدیریت WordPress شوید
2. به قسمت **افزونه‌ها** بروید
3. پلاگین **Honar Maghz Contract** را فعال کنید

### 4. تنظیمات اولیه

فایل `honar-contract.php` را باز کنید و تنظیمات زیر را انجام دهید:

```php
// ایمیل مدیریت (برای دریافت قراردادها)
define('HONAR_ADMIN_EMAIL', 'your-email@domain.com');

// نام سازمان/آموزشگاه
define('HONAR_ORG_NAME', 'نام سازمان شما');
```

### 5. آماده‌سازی فایل قالب Word

1. فایل قرارداد Word خود را در مسیر پلاگین قرار دهید
2. در فایل Word از متغیرهای زیر استفاده کنید:
   - `${PARTNER_NAME}` - نام طرف دوم
   - `${PARTNER_TITLE}` - سمت طرف دوم
   - `${PARTNER_ADDRESS}` - آدرس
   - `${PARTNER_EMAIL}` - ایمیل
   - `${PLAN}` - طرح انتخابی
   - `${DATE}` - تاریخ شمسی
   - `${ORG_NAME}` - نام سازمان

### 6. استفاده از شورت‌کد

فرم قرارداد را در هر صفحه یا پست با شورت‌کد زیر نمایش دهید:

```
[honar_maghz_contract]
```

## بررسی نصب صحیح

1. یک صفحه جدید در WordPress ایجاد کنید
2. شورت‌کد `[honar_maghz_contract]` را در محتوای صفحه قرار دهید
3. صفحه را مشاهده کنید
4. باید فرم قرارداد با تمام فیلدها نمایش داده شود

## عیب‌یابی

### خطا: "کتابخانه تولید PDF یافت نشد"
- مطمئن شوید که `composer install` را اجرا کرده‌اید
- پوشه `vendor` باید در مسیر پلاگین موجود باشد

### خطا: "فایل قالب Word یافت نشد"
- مطمئن شوید فایل `قرارداد رسمی.docx` در مسیر پلاگین وجود دارد
- در صورت تغییر نام فایل، ثابت `HONAR_CONTRACT_TEMPLATE` را بروزرسانی کنید

### PDF تولید نمی‌شود
- مجوز نوشتن در پوشه `wp-content/uploads/contracts` را بررسی کنید
- دسترسی پوشه باید 755 باشد

```bash
chmod 755 wp-content/uploads/contracts
```

## پشتیبانی

برای پشتیبانی و گزارش مشکلات، با تیم توسعه تماس بگیرید.

---

**نسخه:** 2.0  
**تاریخ بروزرسانی:** 1403/07/25  
**توسعه‌دهنده:** تیم هنر مغز
